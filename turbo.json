{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "stream",
  "cacheMaxAge": "14d",
  "cacheMaxSize": "5GB",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**", "*.tsbuildinfo"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^typecheck"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [],
      "env": ["DATABASE_URL", "CI"]
    },
    "test:watch": {
      "cache": false,
      "persistent": true
    },
    "eval:quality": {
      "cache": false,
      "env": ["DATABASE_URL", "AGENT_MODELS", "OPENAI_API_KEY", "CI"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
