// Placeholder — full implementation in subsequent tasks.
export function AdminUserDetail({ userId }: { userId: string }) {
  return <div className="p-6">User detail for {userId} (under construction)</div>;
}
