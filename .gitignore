# Private design & planning docs — kept locally for Claude to reference,
# not published to the shared repo. CLAUDE.md stays tracked.
docs/superpowers
.superpowers/
docs/feature/kpi-metrics
docs/modules/pm-kpi-weekly-spec-coverage.md

# Local Claude Code config (per-machine, not shared)
.claude/settings.local.json
.claude/worktrees/
.claude/*.lock
.claude/skills

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TanStack Router generated files
apps/web/src/routeTree.gen.ts

# Misc
.DS_Store
*.pem

# Compose smoke-test scratch (rendered from .env.example at test time)
.env.smoke

# Traefik ACME storage (per-deploy state, never committed)
infra/traefik/acme.json
# Traefik BasicAuth credentials (generated locally, never committed)
infra/traefik/jaeger-auth

# OpenTofu / Terraform
**/.terraform/
**/.terraform.lock.hcl
**/terraform.tfstate
**/terraform.tfstate.backup
**/terraform.tfstate.d/
**/*.tfvars
!**/*.tfvars.example
**/*.tfplan
**/.tofu/
**/crash.log
**/crash.*.log
.playwright-mcp/
docs/design/
.worktrees/

# Generated PDFs (rendered from docs via scripts/export-pdf.sh)
docs/**/*.pdf

# App NDJSON logs (dev log persistence)
logs/

# TanStack Query generated files
apps/web/.tanstack
apps/web/.tanstack.idea/

# Local IDE metadata
.idea/

# Seed data sourced from a real employee spreadsheet (PII — never committed)
apps/cli/seed/data/employees.csv

# Real employee fixture workbook (real names/emails/ids — never committed)
private/

.agents
skills-lock.json

# Generated at deploy by scripts/write-e2e-route.sh
infra/traefik/dynamic/e2e-report.yml
Makefile
# Local design mockup bundle — reference only, never commit
docs/weekly.html
