# @seta/{{name}}

> TODO: one-paragraph description of the {{name}} module — what domain it owns and what surface it exposes.

## Public surface

- `@seta/{{name}}` — application services (Node)
- `@seta/{{name}}/events` — event type constants + zod payload schemas
- `@seta/{{name}}/rbac` — permission constants
- `@seta/{{name}}/contracts` — browser-safe DTOs + zod schemas
- `@seta/{{name}}/register` — `ContributionRegistry` hook (Node)

## Events emitted

_(none yet)_

## Events consumed

_(none yet)_
