# Local state for the scoped-IAM provisioning test (one-shot apply/destroy,
# run from this machine only). The CI e2e used the bootstrap-sandbox S3 bucket;
# not needed here and it keeps the permission test focused on provisioning.
