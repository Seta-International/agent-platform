apiVersion: 1
datasources:
  - name: Loki
    type: loki
    uid: loki          # stable uid so provisioned dashboards can reference it
    access: proxy
    url: http://loki:3100
    isDefault: false
